{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid && auth.token.email === 'owner@example.com'",
        ".write": "auth != null && auth.uid === $uid && auth.token.email === 'owner@example.com'",
        "categories": { ".indexOn": ["order"] },
        "sources": { ".indexOn": ["order", "categoryId"] },
        "items": { ".indexOn": ["fetchedAt", "publishedAt", "categoryId", "saved", "relevance"] }
      }
    }
  }
}
