{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid && auth.token.email === 'tarunnagasai007@gmail.com'",
        ".write": "auth != null && auth.uid === $uid && auth.token.email === 'tarunnagasai007@gmail.com'",
        "categories": { ".indexOn": ["order"] },
        "sources": { ".indexOn": ["order", "categoryId"] },
        "items": { ".indexOn": ["fetchedAt", "publishedAt", "categoryId", "saved", "relevance"] }
      }
    }
  }
}
